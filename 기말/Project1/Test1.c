#include <stdio.h>
int a;
int plus(int);

int main() {
	int beyul[4] = { 1 };

	for (int i = 0; i < 4; i++) {
		printf("배열의[%d] 값 %d\n", i, beyul[i]);
	}

	return 0;
}

int plus(int a) {

	//static int b = a;

	return 0;
}